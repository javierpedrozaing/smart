<h1>Enviar notificación</h1>
<%= form_for @notif, url: {action: "create"}, method: "post", html: {} do |f| %>
  <% if @notif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notif.errors.count, "error") %> impidieron que la notificación se enviara:</h2>

      <ul>
      <% @notif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
  <%= f.text_area :texto , class: "form-control"%>
  
  </div>
  <div class="form-group">
    <label>DESTINATARIOS</label>
    <p>Seleccione a que usuarios desea enviar la notificación</p>
    <% @roles.each do |rol| %>
    <%= check_box_tag("notificacion_roles[]", rol.id) %>
    <%= label_tag("notificacion_roles[]", rol.rol) %>
    <% end %>
  </div>
  <%= hidden_field_tag('tipo_notif_id',  4) %>
  <div class="actions form-group">
  <%= f.submit "Enviar notificación", class: "btn btn-success" %>
  </div>
<% end %>