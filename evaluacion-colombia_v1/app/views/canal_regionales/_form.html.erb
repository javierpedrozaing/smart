<%= form_for(@canal_regional) do |f| %>
  <% if @canal_regional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@canal_regional.errors.count, "error") %> prohibited this canal_regional from being saved:</h2>

      <ul>
      <% @canal_regional.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
