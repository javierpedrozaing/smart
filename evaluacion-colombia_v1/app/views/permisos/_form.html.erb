<%= form_for(@permiso) do |f| %>
  <% if @permiso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permiso.errors.count, "error") %> prohibited this permiso from being saved:</h2>

      <ul>
      <% @permiso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :perfiles_id %><br>
    <%= f.text_field :perfiles_id %>
  </div>
  <div class="field">
    <%= f.label :estados_id %><br>
    <%= f.text_field :estados_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
